# 中文连续性检查器：第一轮结果

日期：2026-09-08。对应[冻结协议](./continuity-calibration-round01.md)。

结论：实验修改和一次真实对照已完成，但效果不足以接入自动定稿。定向检查比普通审稿多发现一处关键物品交接错误，并更准确地保留缺失历史的不确定性；仍漏掉到期目标和时间风险，且有过度断言。本轮不继续调提示词或追加请求。

## 实际执行与边界

- 生产源码基线：`b96e102657a462b13e579bf2d98cd263e879fd84`。
- 真实实验代码提交：`d8a57ca27af82f88edffc9903b816b436231840f`。后续结果文档提交不改变被测代码。
- 模型：硅基流动 `deepseek-ai/DeepSeek-V4-Flash`，全部中文。沿用已配置的 temperature 0.7、max_tokens 16384；配置只读。
- 10 个固定案例 × 2 臂，20/20 请求完成，HTTP 200、finish_reason=stop；重试 0、改稿 0、新章节生成 0。两臂成对并发，供应商调用阶段共约 115 秒。
- 输入和 gold 在调用前经独立预审冻结，调用后 SHA256 不变。gold 未进入 runner 或提示词；两臂事实材料逐字核对一致，原稿与候选身份在共享资料中明确。
- 普通臂复用生产中文内置模板和 ReviewPromptBuilder，但这是 prompt 级对照，不是完整 ReviewCommand、桌面 UI 或自动定稿验收。
- 未修改 `src`、用户真实小说、数据库、写作提示词或真实 provider 配置。请求与结果扫描未发现配置中的密钥泄漏。

## 逐项语义结果

结果由独立 Astra/medium 读取冻结资料与两臂原始收据复核；不是由被测模型自评分。负例只按预标注的关键错误计命中，风险定位与正确裁决分开。

| 案例 | 分组／类型 | 普通审稿 | 定向检查 |
| --- | --- | --- | --- |
| c01 | 开发／确定错误 | 漏判关键物品持有者回退 | 命中具体交接错误，引文支持 |
| c02 | 保留／正确控制 | 接受中间人转交，无误报 | 无误报 |
| c03 | 开发／待核实风险 | 漏判，还将原稿中的事实混作候选事实 | 找到前缀变后缀，但误称确定矛盾；裁决不合格 |
| c04 | 开发／正确控制 | 接受角色说谎；输出结构不合格 | 无误报 |
| c05 | 保留／待核实风险 | 漏掉时间归属风险；另有错误批评 | 漏判 |
| c06 | 保留／确定错误 | 漏判相册延期完成 | 同样漏判 |
| c07 | 保留／正确控制 | 接受合理知情边界，无误报 | 无误报 |
| c08 | 保留／正确控制 | 接受未来安排，无误报 | 无误报 |
| c09 | 保留／证据不足 | 要求核查缺章，却仍将结论定为矛盾 | 正确保留 unknown，不编造交接 |
| c10 | 保留／正确控制 | 无误报，没有编造诊断 | 无误报 |

分组汇总：

| 指标 | 开发集普通／定向 | 保留集普通／定向 | 合计普通／定向 |
| --- | --- | --- | --- |
| 确定错误命中 | 0/1 ／ 1/1 | 0/1 ／ 0/1 | 0/2 ／ 1/2 |
| 风险正确裁决 | 0/1 ／ 0/1 | 0/1 ／ 0/1 | 0/2 ／ 0/2 |
| 正例语义误报 | 0/1 ／ 0/1 | 0/4 ／ 0/4 | 0/5 ／ 0/5 |
| 证据不足正确保留不确定性 | 无此类案例 | 0/1 ／ 1/1 | 0/1 ／ 1/1 |

c03 定向臂定位到了风险，但不能计为正确裁决：未知完整编号可能同时以前三位和末三位出现同一组数字，现有引文只能支持“新断言缺少依据”。这正说明“引文存在”不等于“推断成立”。

独立复核另发现：

- 普通臂 c05 声称正文未提某个人名，实际正文已有该人名；又要求提前处理一个明确保留到未来的告知承诺。这些批评不能抵算时间风险命中。
- c05 未明确票据属性，确有字面依据，可保留为表达不充分的疑问；不能据此要求新的章节钩子必须已有伏笔。
- 普通臂 c05、c09 部分 quote 合并、改写或加了来源标签，非逐字引文；不全部混称为事实捏造。

## 格式、成本和代码验证

- 两臂各 10/10 JSON 可解析。普通臂 c04 把 summary 放在 item 内，根对象缺少该字段：按提示约定的结构为 9/10，不声称调用了生产解析器验证。
- 定向臂 10/10 通过结构及引文定位校验：7 clear、2 conflict、1 unknown。clear 不是事实正确证明；机械全通过没有消除语义漏判和误判。
- 13/13 针对性 Node 测试通过，无跳过，包括无网络模拟 SSE、失败停止、拒绝重复执行、gold 隔离和配置不改不泄漏。脚本语法检查和 `git diff --check` 通过。
- `git diff b96e102 -- src` 为空；没有重新跑全产品测试、构建或安装包验收。

| 供应商 usage | 普通审稿 | 定向检查 |
| --- | ---: | ---: |
| 输入 tokens | 20,359 | 19,733 |
| 输出 tokens | 11,729 | 10,687 |
| 总 tokens | 32,088 | 30,420 |

这是供应商 usage 口径，未分拆推理 tokens，未换算金额；没有根据小样本估计长期成本或效果显著性。

离线复验命令（不调用真实模型）：

```powershell
node --check scripts/run-continuity-calibration.mjs
node --test scripts/lib/continuity-calibration.node-test.mjs scripts/lib/continuity-calibration-runner.node-test.mjs
git diff --check
```

真实实验只有一次。已执行目录的 execute 被拒绝，失败也不自动重试；新一轮需另行明确范围和额度。先前仅准备、未执行的 prepared-run 目录保留为审查前记录，物理请求数为 0，不并入本轮真实结果。

## Code Review

按 code-review 两轴标准，独立 Astra/medium 审查固定 `b96e102...d8a57ca`，覆盖全部 5 个新增实验文件和 hunks。

### Standards

硬违规 0，需处理的判断性 smell 0。未改生产领域状态；引文定位与语义真实性分离，冻结、收据和执行锁服务本轮有限实验。测试通过证据由主线程提供，审查者没有声称自己重跑测试。

### Spec

初审 1 项文档措辞问题：交替分配请求序号不等于网络顺序交错。已在真实调用前修正文档为同案例两臂并发，复核后剩余问题 0。没有通过修改提示词或样本来处理审查意见。

## 决策与未完成项

保留定向检查作为实验实现，不接入自动定稿。代码审查通过不等于产品质量通过。

本轮支持的系统层判断是：只让审稿模型给一个总体通过结论，无法确认它逐项检查了本章必须完成的事情；增加引文也不能自动保证推断正确。后续最小实验应优先验证“逐项列出本章到期目标，并给出完成证据或未完成/未知”，而不是继续叠加通用审稿规则或无限整章重写。该后续方案尚未实现、未验证，不能算作本轮修复成果。

本轮没有生成章节，未验证字数、长篇漂移改善、局部改稿质量、生产流程、Issue #199、Windows/macOS 安装包或 1.1.0 发布资格。未推送、提交 PR、合并、发布、打 tag 或关闭 Issue。

样本只有 10 例，部分同一小说背景，负例正文较长、正确控制多为节选；保留集不是独立新小说分布。不能把本轮结果推为一般长篇质量，也不能据此裁决 max_tokens 是否造成字数偏差。

## 本地证据索引

原始合成正文、冻结标签和 API 收据留在当前工作树的 ignored 目录，不进入代码提交：

`.runtime/.cache/continuity-calibration-20260908-round01/`

- `inputs.json`：`1b10053a76f8ab97734545ffbaef9704c03cebc82b1050f26deed730ef582baf`
- `gold.json`：`95114decef2fce14e7f1f0a8a644e5eca5a7443d113c14b5a01e314e3e4e4004`
- `prepared-reviewed/prepared.json`：`f0bf1001acfbac3713d62711e506e416f17c3c466660ceaca2a5f32be5574adf`
- `prepared-reviewed/manifest.json`：`7282cb3c1493e9d0483746e569a726ceef2c28ef7a8ae7b326e124d0c9b8c4cf`
- 每个案例目录的 `baseline.json` 和 `focused.json` 保留输入提示、参数、usage、耗时、状态及原始输出，不含请求授权头。

本轮未创建仓库外临时目录；测试自建的临时夹具已清理，真实实验与预审证据保留供复核。
